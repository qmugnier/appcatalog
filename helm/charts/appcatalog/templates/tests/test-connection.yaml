apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appcatalog.fullname" . }}-test-connection"
  labels:
    {{- include "appcatalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appcatalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
